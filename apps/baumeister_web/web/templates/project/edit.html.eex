<h2>Edit project</h2>

<%= render "form.html", changeset: @changeset,
                        action: project_path(@conn, :update, @project) %>

<%= link ["Overview ", glyph "th-list"], to: project_path(@conn, :index), class: "btn btn-default" %>
